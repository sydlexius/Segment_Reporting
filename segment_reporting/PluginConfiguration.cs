using MediaBrowser.Model.Plugins;

namespace segment_reporting
{
    /// <summary>
    /// Required by the Emby plugin SDK. Currently empty as the plugin stores
    /// all data in its own SQLite cache and has no user-facing settings.
    /// </summary>
    public class PluginConfiguration : BasePluginConfiguration
    {
    }
}
