meta {
  name: Delete Segment - Missing ItemId (Should Fail)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/emby/segment_reporting/delete_segment?MarkerType=IntroStart
  body: none
  auth: none
}

headers {
  X-Emby-Token: {{apiKey}}
}

docs {
  ## Delete Segment - Validation Test

  Tests that omitting the required `ItemId` parameter returns an error.

  **Expected Response:**
  ```json
  {
    "error": "itemId is required"
  }
  ```

  **Acceptance Criteria:**
  - ✅ Returns error message when ItemId is missing
}
