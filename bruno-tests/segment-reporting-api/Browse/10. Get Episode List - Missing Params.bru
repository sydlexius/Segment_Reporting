meta {
  name: Get Episode List - Missing Params (Should Fail)
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/emby/segment_reporting/episode_list
  body: none
  auth: none
}

headers {
  X-Emby-Token: {{apiKey}}
}

docs {
  ## Get Episode List - Validation Test

  Tests that omitting both `seasonId` and `seriesId` returns an error.

  **Expected Response:**
  ```json
  {
    "error": "Either seasonId or seriesId is required"
  }
  ```

  **Acceptance Criteria:**
  - ✅ Returns error when both params are missing
}
