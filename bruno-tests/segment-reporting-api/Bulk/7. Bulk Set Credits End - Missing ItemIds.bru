meta {
  name: Bulk Set Credits End - Missing ItemIds (Should Fail)
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/emby/segment_reporting/bulk_set_credits_end
  body: none
  auth: none
}

headers {
  X-Emby-Token: {{apiKey}}
}

docs {
  ## Bulk Set Credits End - Validation Test

  Tests that omitting the required `ItemIds` parameter returns an error.

  **Expected Response:**
  ```json
  {
    "error": "itemIds is required"
  }
  ```

  **Acceptance Criteria:**
  - ✅ Returns error for missing itemIds
}
